#include <iostream>
#include <algorithm>
#include <math.h>
#define MAXN 10005
#define eps 0.000001
using namespace std;

int arr[MAXN], sum[MAXN], n, m, tmax;

int main(void) {
	freopen("测试数据", "r", stdin);
	
	return 0;
}
