#include <iostream>
#include <vector>
#include <stack>
#include <math.h>
#include <map>
#include <set>
#include <string.h>
#include <algorithm>
#define MAXN 100005
using namespace std;

bool check(string& line, int lef, int rig) {
	int len = l.size() >> 1;
	for(int l=lef, r=rig; l<len && r>len; l++, r--) {
		if()
	}
}

int main() { 
	freopen("test", "r", stdin);
	
	string line;
	getline(cin, line);
	
	
	return 0;
}


