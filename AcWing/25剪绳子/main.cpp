#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
using namespace std;
#define MAXN 100005
#define debug

class Solution {
public:
    int maxProductAfterCutting(int length) {
		for(int i=0;)
    }
};

#ifdef debug
int main(void) {
	Solution s;
	cout << s.maxProductAfterCutting(8);
	return 0;
}
#endif
