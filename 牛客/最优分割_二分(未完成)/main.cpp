#include <iostream>
#include <set>
#include <vector>
#include <queue>
#define MAXN 9999999
using namespace std;

int n, m, arr[MAXN];

int main(void) {
	freopen("test", "r", stdin);
	scanf("%d %d", &n, &m);
	for(int i=1; i<=n; i++)
		scanf("%d", arr+i);
	int lef = 0; rig = 
	return 0;
}
